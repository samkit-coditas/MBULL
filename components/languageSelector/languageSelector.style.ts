import { styled } from "@mui/material/styles";

export const MainContainer = styled("div")(
  ({ theme }: any) => `
    background-color: #fff;
    padding:1rem;
    .container{
    };
    .selectElement{
       margin-top:1rem;
       width:6rem;
    }
        `
);
